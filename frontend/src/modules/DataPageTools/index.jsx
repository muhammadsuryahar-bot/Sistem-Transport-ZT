import { useMemo, useRef, useState } from 'react'
import { supabase } from '../../lib/supabase'
import '../DataPageTools.css'

const clean = value => String(value ?? '').replace(/\s+/g, ' ').trim()
const norm = value => clean(value).toLowerCase().replace(/[^a-z0-9]+/g, '_').replace(/^_|_$/g, '')
const CONTEXT_LABEL = { kendaraan: 'Kendaraan', pengajuan: 'Pengajuan Service', service: 'Service & Perbaikan', sewa: 'Kendaraan Sewa', dokumen: 'Dokumen Kendaraan' }
const RELEVANT_SHEETS = {
  kendaraan: ['kendaraan', 'list kendaraan', 'data kendaraan'], service: ['data service', 'service'],
  dokumen: ['stnk', 'stnk dan kir', 'kir'], pengajuan: ['pengajuan perbaikan', 'permintaan perbaikan', 'permintaan service'], sewa: ['sewa kendaraan', 'kendaraan sewa'],
}
const aliases = {
  nomor_polisi:['nomor_polisi','no_polisi','no_polisi_','no_polisi_kendaraan','no_pol','plat','no_plat'], merk:['merk','brand'], tipe:['tipe','type'], jenis_kendaraan:['jenis','jenis_kendaraan'],
  tahun:['tahun'], nomor_mesin:['no_mesin','nomor_mesin'], nomor_rangka:['no_rangka','nomor_rangka','no_ranka'], pemilik:['pemilik','nama_pemilik','nama_pemilik_kendaraan','nama_pemilik_pic'],
  lokasi:['lokasi','lokasi_kerja','lokasi_kerja_','homebase','home_base'], kode_kendaraan:['kode_kendaraan','kode'], kilometer:['km','kilometer','kilometer_pengajuan','km_terakhir'],
  tanggal:['tanggal','tgl','tanggal_service','tanggal_pengajuan'], jenis_permintaan:['jenis_permintaan','jenis_perbaikan','jenis_service'], keluhan:['keluhan','keluhan_kerusakan','uraian_kerusakan','uraian'], prioritas:['prioritas'],
  jenis_pekerjaan:['jenis_pekerjaan','jenis_pekerjan','jenis_pekerjaan_'], bengkel:['nama_bengkel','bengkel','nama_bengkel_service'], qty:['qty','jumlah'], satuan:['sat','satuan','unit'],
  harga_satuan:['harga_satuan_rp','harga_satuan','harga'], nilai_dpp:['nilai_dpp','dpp'], ppn:['ppn','ppn_'], total:['total','jumlah_rp','jumlah'], keterangan:['keterangan','catatan'], stnk:['stnk'], kir:['kir'], cinco:['5_tahun','lima_tahun'],
  nomor_kontrak:['nomor_kontrak','no_kontrak'], nama_perusahaan:['nama_perusahaan','perusahaan'], jenis_pemilik:['jenis_pemilik'], tanggal_mulai:['tanggal_mulai','mulai'], tanggal_selesai:['tanggal_selesai','selesai'],
  nilai_sewa_bulanan:['nilai_sewa_bulanan','sewa_bulanan','harga_sewa'], tanggal_jatuh_tempo_bulanan:['tanggal_jatuh_tempo_bulanan','jatuh_tempo_bulanan'], driver:['driver','nama_driver'],
}
function u16(v,o){return v.getUint16(o,true)} function u32(v,o){return v.getUint32(o,true)} function text(b){return new TextDecoder('utf-8').decode(b)}
async function inflate(bytes){if(typeof DecompressionStream==='undefined') throw new Error('Browser belum mendukung pembacaan XLSX. Gunakan Chrome atau Edge terbaru.'); const s=new Blob([bytes]).stream().pipeThrough(new DecompressionStream('deflate-raw')); return new Uint8Array(await new Response(s).arrayBuffer())}
async function unzip(buffer){const v=new DataView(buffer),b=new Uint8Array(buffer);let e=-1;for(let i=b.length-22;i>=0;i--)if(u32(v,i)===0x06054b50){e=i;break}if(e<0)throw new Error('File bukan XLSX yang valid atau file rusak.');const count=u16(v,e+10),offset=u32(v,e+16),entries=new Map();let p=offset;for(let i=0;i<count;i++){if(u32(v,p)!==0x02014b50)throw new Error('Struktur ZIP XLSX tidak valid.');const method=u16(v,p+10),size=u32(v,p+20),nl=u16(v,p+28),el=u16(v,p+30),cl=u16(v,p+32),lo=u32(v,p+42);const name=text(b.slice(p+46,p+46+nl)),lv=new DataView(buffer,lo),ln=u16(lv,26),le=u16(lv,28),start=lo+30+ln+le;entries.set(name,{method,bytes:b.slice(start,start+size)});p+=46+nl+el+cl}return {read:async name=>{const x=entries.get(name);if(!x)return null;if(x.method===0)return x.bytes;if(x.method===8)return inflate(x.bytes);throw new Error(`Metode kompresi XLSX ${x.method} belum didukung.`)}}}
function colIndex(name){let n=0;for(const c of name)n=n*26+c.charCodeAt(0)-64;return n-1}
function parseSheetXml(xml,shared){const d=new DOMParser().parseFromString(xml,'application/xml');if(d.querySelector('parsererror'))throw new Error('Sheet XLSX tidak dapat dibaca.');const rows=[];d.querySelectorAll('sheetData > row').forEach(row=>{const values=[];row.querySelectorAll(':scope > c').forEach(cell=>{const ref=cell.getAttribute('r')||'',m=ref.match(/^([A-Z]+)/);if(!m)return;const idx=colIndex(m[1]),type=cell.getAttribute('t')||'';let value=clean(cell.querySelector('v')?.textContent);if(type==='s')value=shared[Number(value)]||'';else if(type==='inlineStr')value=clean(cell.querySelector('is')?.textContent);else if(type==='b')value=value==='1'?'TRUE':'FALSE';values[idx]=value});rows.push(values)});return rows}
async function parseXlsx(file){const z=await unzip(await file.arrayBuffer()),workbookXml=text(await z.read('xl/workbook.xml')||new Uint8Array()),relsXml=text(await z.read('xl/_rels/workbook.xml.rels')||new Uint8Array());if(!workbookXml||!relsXml)throw new Error('Workbook XLSX tidak lengkap.');const workbook=new DOMParser().parseFromString(workbookXml,'application/xml'),rels=new DOMParser().parseFromString(relsXml,'application/xml'),shared=[];const sx=await z.read('xl/sharedStrings.xml');if(sx){const d=new DOMParser().parseFromString(text(sx),'application/xml');d.querySelectorAll('si').forEach(si=>shared.push(clean(Array.from(si.querySelectorAll('t')).map(t=>t.textContent).join(''))))}const relMap=Object.fromEntries(Array.from(rels.querySelectorAll('Relationship')).map(r=>[r.getAttribute('Id'),r.getAttribute('Target')]));const sheets=[];for(const s of Array.from(workbook.querySelectorAll('sheets > sheet'))){const t0=relMap[s.getAttribute('r:id')];if(!t0)continue;const target=t0.startsWith('xl/')?t0:`xl/${t0.replace(/^\//,'')}`;sheets.push({name:s.getAttribute('name')||target,rows:parseSheetXml(text(await z.read(target)||new Uint8Array()),shared)})}return sheets}
function findHeader(rows){let best={index:0,row:[]};rows.slice(0,30).forEach((row,idx)=>{const score=row.filter(Boolean).map(norm).filter(Boolean).length;if(score>best.row.filter(Boolean).length)best={index:idx,row}});return best}
function valueOf(row,headers,key){const i=headers.findIndex(h=>(aliases[key]||[]).includes(norm(h)));return i>=0?clean(row[i]):''}
function excelDate(value){const v=clean(value);if(!v)return null;if(/^\d{4}-\d{2}-\d{2}$/.test(v))return v;if(/^\d{1,2}[/\-]\d{1,2}[/\-]\d{4}$/.test(v)){const [d,m,y]=v.split(/[/\-]/);return `${y}-${String(m).padStart(2,'0')}-${String(d).padStart(2,'0')}`}const n=Number(v);if(Number.isFinite(n)&&n>20000&&n<80000)return new Date(Date.UTC(1899,11,30)+n*86400000).toISOString().slice(0,10);const dt=new Date(v);return Number.isNaN(dt.getTime())?null:dt.toISOString().slice(0,10)}
function numberValue(value){const v=clean(value);if(!v)return null;if(/^\d{1,3}(?:\.\d{3})+(?:,\d+)?$/.test(v))return Number(v.replace(/\./g,'').replace(',','.'));const n=Number(v.replace(/,/g,''));return Number.isFinite(n)?n:null}
function likelyContext(name,rows,wanted){const n=norm(name);if(RELEVANT_SHEETS[wanted].some(k=>n.includes(norm(k))))return true;const h=findHeader(rows).row.map(norm),score={kendaraan:['nomor_polisi','no_polisi','merk','type','tipe'].filter(x=>h.includes(x)).length,service:['jenis_pekerjaan','uraian','qty','sat','harga_satuan','nilai_dpp'].filter(x=>h.includes(x)).length,dokumen:['nomor_polisi','stnk','kir','5_tahun'].filter(x=>h.includes(x)).length,pengajuan:['nomor_polisi','keluhan','jenis_permintaan','kilometer'].filter(x=>h.includes(x)).length,sewa:['nomor_kontrak','nomor_polisi','nama_pemilik','nilai_sewa_bulanan'].filter(x=>h.includes(x)).length};return score[wanted]>=(wanted==='kendaraan'?2:3)}
function normalizeVehicleRow(row,headers,driverByName,existingCodes,current){const plate=valueOf(row,headers,'nomor_polisi').toUpperCase(),brand=valueOf(row,headers,'merk');const sourceStatusIndex=headers.findIndex(h=>norm(h)==='status'),sourceStatus=clean(sourceStatusIndex>=0?row[sourceStatusIndex]:'').toUpperCase(),isRental=sourceStatus==='SEWA';const owner=valueOf(row,headers,'pemilik')||null,ownerUpper=clean(owner).toUpperCase();const rentalType=isRental?(/^(PT|CV|UD|PD|KOPERASI|YAYASAN)\b|\bRENTAL\b|\bCAR\s+RENTAL\b/.test(ownerUpper)?'SEWA_RENTAL':'SEWA_PERORANGAN'):null;const driverId=driverByName[clean(valueOf(row,headers,'driver')).toUpperCase()]?.id||null;let code=(valueOf(row,headers,'kode_kendaraan')||`IMP-${plate.replace(/\W+/g,'')}`).toUpperCase();if(!current&&existingCodes.has(code))code=`${code}-${plate.replace(/\W+/g,'')}`;if(!current)existingCodes.add(code);return {existingId:current?.id||null,payload:{kode_kendaraan:current?(valueOf(row,headers,'kode_kendaraan')||current.kode_kendaraan).toUpperCase():code,nomor_polisi:plate,merk:brand,tipe:valueOf(row,headers,'tipe')||null,jenis_kendaraan:valueOf(row,headers,'jenis_kendaraan')||null,tahun:numberValue(valueOf(row,headers,'tahun'))||null,nomor_mesin:valueOf(row,headers,'nomor_mesin')||null,nomor_rangka:valueOf(row,headers,'nomor_rangka')||null,pemilik:owner,driver_id:driverId,lokasi:valueOf(row,headers,'lokasi')||null,kilometer_terakhir:numberValue(valueOf(row,headers,'kilometer'))??0,kepemilikan:isRental?'SEWA':'ASET_KANTOR',jenis_sewa:rentalType,status:'ACTIVE',keterangan:valueOf(row,headers,'keterangan')||null}}}
async function importKendaraan(sheet,profile){if(!profile?.id)throw new Error('Profil pengguna belum tersedia. Silakan login ulang.');const {index,row:headers}=findHeader(sheet.rows),rows=sheet.rows.slice(index+1).filter(r=>r.some(v=>clean(v)));if(!headers.some(h=>['nomor_polisi','no_polisi','plat','no_plat'].includes(norm(h))))throw new Error('Kolom Nomor Polisi tidak ditemukan pada sheet kendaraan.');if(!headers.some(h=>['merk','brand'].includes(norm(h))))throw new Error('Kolom Merk/Brand tidak ditemukan pada sheet kendaraan.');const [vr,dr]=await Promise.all([supabase.from('kendaraan').select('id,nomor_polisi,kode_kendaraan'),supabase.from('driver').select('id,nama_lengkap')]);if(vr.error)throw vr.error;if(dr.error)throw dr.error;const existingByPlate=Object.fromEntries((vr.data||[]).map(v=>[clean(v.nomor_polisi).toUpperCase(),v])),existingCodes=new Set((vr.data||[]).map(v=>clean(v.kode_kendaraan).toUpperCase()).filter(Boolean)),driverByName=Object.fromEntries((dr.data||[]).map(d=>[clean(d.nama_lengkap).toUpperCase(),d])),seen=new Set(),prepared=[],skipped=[];for(let i=0;i<rows.length;i++){const row=rows[i],plate=valueOf(row,headers,'nomor_polisi').toUpperCase();if(!plate||seen.has(plate)){skipped.push({row:i+index+2,reason:'Nomor polisi kosong atau duplikat dalam file.'});continue}const brand=valueOf(row,headers,'merk');if(!brand){skipped.push({row:i+index+2,reason:'Merk kosong.'});continue}seen.add(plate);prepared.push(normalizeVehicleRow(row,headers,driverByName,existingCodes,existingByPlate[plate]))}if(!prepared.length)throw new Error('Tidak ada baris kendaraan valid yang siap diimport.');let added=0,updated=0;for(const item of prepared){const r=item.existingId?await supabase.from('kendaraan').update(item.payload).eq('id',item.existingId):await supabase.from('kendaraan').insert(item.payload);if(r.error)throw new Error(`Gagal menyimpan kendaraan ${item.payload.nomor_polisi}: ${r.error.message}`);item.existingId?updated++:added++}return `${added} kendaraan baru, ${updated} diperbarui, ${skipped.length} dilewati.`}
async function importService(sheet,profile){const {index, row:headers}=findHeader(sheet.rows),rows=sheet.rows.slice(index+1).filter(r=>r.some(v=>clean(v)));const vr=await supabase.from('kendaraan').select('id,nomor_polisi,kilometer_terakhir');if(vr.error)throw vr.error;const vmap=Object.fromEntries((vr.data||[]).map(v=>[clean(v.nomor_polisi).toUpperCase(),v])),groups=new Map();for(const row of rows){const plate=valueOf(row,headers,'nomor_polisi').toUpperCase(),date=excelDate(valueOf(row,headers,'tanggal')),shop=valueOf(row,headers,'bengkel');if(!plate||!date||!vmap[plate])continue;const key=`${plate}|${date}|${shop.toUpperCase()}`;if(!groups.has(key))groups.set(key,[]);groups.get(key).push(row)}let created=0,skipped=0,items=0;for(const group of groups.values()){const first=group[0],plate=valueOf(first,headers,'nomor_polisi').toUpperCase(),vehicle=vmap[plate],date=excelDate(valueOf(first,headers,'tanggal')),shop=valueOf(first,headers,'bengkel')||null;const dup=await supabase.from('service').select('id').eq('kendaraan_id',vehicle.id).eq('tanggal_service',date).eq('bengkel',shop).limit(1);if(dup.error)throw dup.error;if(dup.data?.length){skipped++;continue}const dpp=group.reduce((s,r)=>s+(numberValue(valueOf(r,headers,'nilai_dpp'))||0),0),ppn=group.reduce((s,r)=>s+(numberValue(valueOf(r,headers,'ppn'))||0),0),total=group.reduce((s,r)=>s+(numberValue(valueOf(r,headers,'total'))||0),0)||dpp+ppn,km=numberValue(valueOf(first,headers,'kilometer'))||0,complaint=valueOf(first,headers,'keluhan')||'Import histori service dari Excel.',typeRaw=group.map(r=>valueOf(r,headers,'jenis_pekerjaan')).join(' ').toUpperCase(),jenis=/BAN/.test(typeRaw)?'GANTI_BAN':/AKI|BATERAI/.test(typeRaw)?'GANTI_AKI':'SERVICE';const req=await supabase.from('permintaan_service').insert({pemohon_id:profile.id,kendaraan_id:vehicle.id,tanggal_pengajuan:date,kilometer_pengajuan:km,jenis_permintaan:jenis,keluhan:complaint,prioritas:'NORMAL',status:'MENUNGGU_TRANSPORT'}).select('id').single();if(req.error)throw req.error;const svc=await supabase.from('service').insert({nomor_service:`IMP-SRV-${Date.now()}-${created+1}`,permintaan_service_id:req.data.id,kendaraan_id:vehicle.id,tanggal_service:date,kilometer:km,bengkel:shop,jenis_service:jenis,keluhan:complaint,estimasi_biaya:dpp,biaya_aktual:total,status:'DRAFT',diproses_oleh:profile.id,nilai_dpp:dpp,ppn,total,catatan:`Import histori Excel: ${sheet.name}`}).select('id').single();if(svc.error)throw svc.error;const itemRows=group.map(row=>({service_id:svc.data.id,nama_item:valueOf(row,headers,'keluhan')||valueOf(row,headers,'jenis_pekerjaan')||'Item Excel',kategori:/JASA/i.test(valueOf(row,headers,'jenis_pekerjaan'))?'JASA_SERVICE':/PENGADAAN/i.test(valueOf(row,headers,'jenis_pekerjaan'))?'PENGADAAN_BARANG':/BAN/i.test(valueOf(row,headers,'keluhan'))?'BAN':/AKI|BATERAI/i.test(valueOf(row,headers,'keluhan'))?'AKI_BATERAI':'MATERIAL_SPAREPART',jumlah:numberValue(valueOf(row,headers,'qty'))||1,satuan:valueOf(row,headers,'satuan')||'pcs',harga_satuan:numberValue(valueOf(row,headers,'harga_satuan'))||0,subtotal:numberValue(valueOf(row,headers,'nilai_dpp'))||0,keterangan:valueOf(row,headers,'keterangan')||null}));if(itemRows.length){const r=await supabase.from('service_item').insert(itemRows);if(r.error)throw r.error;items+=itemRows.length}const ru=await supabase.from('permintaan_service').update({status:'DALAM_PROSES',diproses_oleh:profile.id,diproses_at:new Date().toISOString()}).eq('id',req.data.id);if(ru.error)throw ru.error;created++}return `${created} transaksi service dibuat, ${items} item tercatat, ${skipped} transaksi dilewati karena duplikat.`}
async function importDokumen(sheet){const {index,row:headers}=findHeader(sheet.rows),rows=sheet.rows.slice(index+1).filter(r=>r.some(v=>clean(v))),vr=await supabase.from('kendaraan').select('id,nomor_polisi');if(vr.error)throw vr.error;const vmap=Object.fromEntries((vr.data||[]).map(v=>[clean(v.nomor_polisi).toUpperCase(),v]));let inserted=0,skipped=0;for(const row of rows){const plate=valueOf(row,headers,'nomor_polisi').toUpperCase(),vehicle=vmap[plate];if(!vehicle){skipped++;continue}for(const [key,type] of [['stnk','STNK'],['kir','KIR'],['cinco','5_TAHUN']]){const due=excelDate(valueOf(row,headers,key));if(!due)continue;const dup=await supabase.from('dokumen_kendaraan').select('id').eq('kendaraan_id',vehicle.id).eq('jenis_dokumen',type).eq('tanggal_jatuh_tempo',due).limit(1);if(dup.error)throw dup.error;if(dup.data?.length){skipped++;continue}const r=await supabase.from('dokumen_kendaraan').insert({kendaraan_id:vehicle.id,jenis_dokumen:type,tanggal_jatuh_tempo:due,keterangan:`Import Excel: ${sheet.name}`});if(r.error)throw r.error;inserted++}}return `${inserted} dokumen ditambahkan, ${skipped} baris/dokumen dilewati.`}
async function importPengajuan(sheet,profile){const {index,row:headers}=findHeader(sheet.rows),rows=sheet.rows.slice(index+1).filter(r=>r.some(v=>clean(v))),vr=await supabase.from('kendaraan').select('id,nomor_polisi,kilometer_terakhir');if(vr.error)throw vr.error;const vmap=Object.fromEntries((vr.data||[]).map(v=>[clean(v.nomor_polisi).toUpperCase(),v]));let added=0,skipped=0;for(const row of rows){const plate=valueOf(row,headers,'nomor_polisi').toUpperCase(),vehicle=vmap[plate],complaint=valueOf(row,headers,'keluhan');if(!vehicle||!complaint){skipped++;continue}const date=excelDate(valueOf(row,headers,'tanggal'))||new Date().toISOString().slice(0,10),km=numberValue(valueOf(row,headers,'kilometer'))??vehicle.kilometer_terakhir??0,r=await supabase.from('permintaan_service').insert({pemohon_id:profile.id,kendaraan_id:vehicle.id,tanggal_pengajuan:date,kilometer_pengajuan:km,jenis_permintaan:valueOf(row,headers,'jenis_permintaan')||'SERVICE',keluhan:complaint,prioritas:valueOf(row,headers,'prioritas')||'NORMAL',status:'MENUNGGU_TRANSPORT'});if(r.error)throw r.error;added++}return `${added} pengajuan ditambahkan, ${skipped} baris dilewati.`}
async function importSewa(sheet,profile){const {index,row:headers}=findHeader(sheet.rows),rows=sheet.rows.slice(index+1).filter(r=>r.some(v=>clean(v)));const required=['nomor_kontrak','nomor_polisi','tanggal_mulai','tanggal_selesai','nilai_sewa_bulanan'];if(!required.every(k=>headers.map(norm).includes(k)))throw new Error('Format import rental harus memiliki Nomor Kontrak, Nomor Polisi, Tanggal Mulai, Tanggal Selesai, dan Nilai Sewa Bulanan.');const vr=await supabase.from('kendaraan').select('id,nomor_polisi');if(vr.error)throw vr.error;const vmap=Object.fromEntries((vr.data||[]).map(v=>[clean(v.nomor_polisi).toUpperCase(),v]));let added=0,skipped=0;for(const row of rows){const plate=valueOf(row,headers,'nomor_polisi').toUpperCase(),vehicle=vmap[plate],nomorKontrak=valueOf(row,headers,'nomor_kontrak');if(!vehicle||!nomorKontrak){skipped++;continue}const ownerName=valueOf(row,headers,'pemilik')||valueOf(row,headers,'nama_pemilik')||'-';let owner=await supabase.from('pemilik_sewa').select('id').eq('nama_pemilik',ownerName).maybeSingle();if(owner.error)throw owner.error;if(!owner.data){const created=await supabase.from('pemilik_sewa').insert({jenis_pemilik:valueOf(row,headers,'jenis_pemilik')||'PERORANGAN',nama_pemilik:ownerName,nama_perusahaan:valueOf(row,headers,'nama_perusahaan')||null,aktif:true}).select('id').single();if(created.error)throw created.error;owner=created}const r=await supabase.from('kontrak_sewa').insert({nomor_kontrak:nomorKontrak,kendaraan_id:vehicle.id,pemilik_sewa_id:owner.data.id,tanggal_mulai:excelDate(valueOf(row,headers,'tanggal_mulai')),tanggal_selesai:excelDate(valueOf(row,headers,'tanggal_selesai')),periode_bulan:6,nilai_sewa_bulanan:numberValue(valueOf(row,headers,'nilai_sewa_bulanan'))||0,tanggal_jatuh_tempo_bulanan:excelDate(valueOf(row,headers,'tanggal_jatuh_tempo_bulanan')),status:'AKTIF',catatan:`Import Excel: ${sheet.name}`,dibuat_oleh:profile.id});if(r.error)throw r.error;added++}return `${added} kontrak sewa ditambahkan, ${skipped} baris dilewati.`}
function ExcelImportModal({context,profile,onDone,onClose}){const inputRef=useRef(null),[file,setFile]=useState(null),[sheets,setSheets]=useState([]),[selected,setSelected]=useState(null),[loading,setLoading]=useState(false),[saving,setSaving]=useState(false),[error,setError]=useState(''),[message,setMessage]=useState('');const canImport=['ADMIN','TRANSPORT'].includes(profile?.role);const scan=async f=>{setFile(f);setSheets([]);setSelected(null);setError('');setMessage('');if(!f)return;if(!/\.xlsx$/i.test(f.name)){setError('Gunakan file .xlsx.');return}setLoading(true);try{const parsed=await parseXlsx(f);setSheets(parsed.map(s=>{const {index,row}=findHeader(s.rows);return {...s,headerIndex:index,headers:row,relevant:likelyContext(s.name,s.rows,context)}}));setMessage(`${parsed.length} sheet dibaca. Pilih sheet yang sesuai dengan ${CONTEXT_LABEL[context]}.`)}catch(e){setError(e.message||'File Excel tidak dapat dibaca.')}finally{setLoading(false)}};const relevant=useMemo(()=>sheets.filter(s=>s.relevant),[sheets]);const start=async()=>{if(!selected||!canImport)return;setSaving(true);setError('');setMessage('');try{let result;if(context==='kendaraan')result=await importKendaraan(selected,profile);else if(context==='service')result=await importService(selected,profile);else if(context==='dokumen')result=await importDokumen(selected,profile);else if(context==='pengajuan')result=await importPengajuan(selected,profile);else result=await importSewa(selected,profile);setMessage(`Import ${CONTEXT_LABEL[context]} berhasil. ${result}`);onDone?.()}catch(e){setError(e.message||'Import gagal. Data yang sudah tersimpan sebelum error mungkin perlu diperiksa.')}finally{setSaving(false)}};return <div className="dpt-overlay" role="dialog" aria-modal="true" aria-label={`Import ${CONTEXT_LABEL[context]}`}><section className="dpt-modal"><header><div><span className="eyebrow">IMPORT EXCEL</span><h3>Import {CONTEXT_LABEL[context]}</h3><p>Upload → preview → validasi → cek duplikat → import.</p></div><button type="button" className="dpt-icon" onClick={onClose}>×</button></header>{error&&<div className="dpt-alert error">{error}</div>}{message&&<div className="dpt-alert success">{message}</div>}<div className="dpt-upload"><input ref={inputRef} type="file" accept=".xlsx" onChange={e=>scan(e.target.files?.[0])}/><button type="button" className="dpt-button primary" onClick={()=>inputRef.current?.click()} disabled={loading}>{loading?'Membaca Excel...':file?file.name:'Pilih File Excel (.xlsx)'}</button></div>{file&&<><div className="dpt-sheet-list"><div className="dpt-sheet-title"><b>Sheet ditemukan</b><span>{sheets.length} sheet</span></div>{sheets.map((s,i)=><button type="button" key={`${s.name}-${i}`} className={`dpt-sheet ${selected?.name===s.name?'selected':''}`} onClick={()=>setSelected(s)}><div><b>{s.name}</b><small>{Math.max(0,s.rows.length-s.headerIndex-1)} baris data • {s.relevant?'cocok':'tidak diprioritaskan'}</small></div><span>{s.relevant?'✓':'•'}</span></button>)}</div>{relevant.length===0&&<div className="dpt-note">Sheet yang ditemukan belum cocok untuk <b>{CONTEXT_LABEL[context]}</b>.</div>}{selected&&<div className="dpt-preview"><div className="dpt-sheet-title"><b>Preview: {selected.name}</b><span>maks. 8 baris</span></div><div className="dpt-preview-wrap"><table><thead><tr>{selected.headers.map((h,i)=><th key={`${h}-${i}`}>{h||`Kolom ${i+1}`}</th>)}</tr></thead><tbody>{selected.rows.slice(selected.headerIndex+1,selected.headerIndex+9).map((row,r)=><tr key={r}>{selected.headers.map((_,c)=><td key={c}>{clean(row[c])||'-'}</td>)}</tr>)}</tbody></table></div></div>}<div className="dpt-actions"><button type="button" className="dpt-button" onClick={onClose}>Batal</button><button type="button" className="dpt-button primary" onClick={start} disabled={!selected||saving||!canImport}>{saving?'Mengimport...':`Import ${CONTEXT_LABEL[context]}`}</button></div></>}</section></div>}
export default function DataPageTools({context,profile,onExport}){const [showImport,setShowImport]=useState(false),[exporting,setExporting]=useState(false);const canImport=['ADMIN','TRANSPORT'].includes(profile?.role);const doExport=async()=>{if(!onExport)return;setExporting(true);try{await onExport()}finally{setExporting(false)}};if(!CONTEXT_LABEL[context])return null;return <>{<div className="dpt-toolbar"><div><span className="eyebrow">DATA</span><b>{CONTEXT_LABEL[context]}</b></div><div className="dpt-toolbar-actions">{canImport&&<button className="dpt-button secondary" type="button" onClick={()=>setShowImport(true)}>⇧ Import Excel</button>}<button className="dpt-button primary" type="button" onClick={doExport} disabled={exporting}>⇩ {exporting?'Exporting...':'Export Excel'}</button></div></div>}{showImport&&<ExcelImportModal context={context} profile={profile} onClose={()=>setShowImport(false)} onDone={()=>{setShowImport(false);window.setTimeout(()=>window.location.reload(),400)}}/>}</>}

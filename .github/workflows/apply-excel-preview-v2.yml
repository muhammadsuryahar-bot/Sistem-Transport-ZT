name: Apply Excel preview v2

on:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  patch-and-verify:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "Legacy Excel preview patch workflow frozen; verified wiring is already committed."
